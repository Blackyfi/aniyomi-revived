# Aniyomi-Revived — Work Backlog (for concurrent agents)

> **Execution status — 2026-05-29 (concurrent Opus 4.8 agents).** All 15 tasks ran. Summary:
>
> | Task | Status | Evidence |
> |---|---|---|
> | **P0** SDK install | ✅ DONE | `gradlew -q help` exit 0; `aapt2 2.19`; build-tools 35.0.0 + platform-35 + platform-tools 37 @ `C:\Android\sdk`; `local.properties` in both repos |
> | **A1** Repoint updater | ✅ DONE (confirm owner) | `AppUpdateChecker.kt:43-49` → fork; asset-name dep at `ReleaseServiceImpl.kt:37-46`. Merged to APP `main`. ⚠️ owner name Blackyfi vs nicolasticot — see Q1 |
> | **A2** Mirror JitPack deps | ✅ DONE (plan) | `A2_MIRROR_PLAN.md`: all 5 upstreams archived; fork-and-tag plan + exact catalog diffs. No repos created |
> | **A3** Transitive OSV scan | ✅ DONE | 31 transitive advisories / 15 pkgs; only **guava 31.0.1-jre** is shipped (1 Mod+1 Low); appendix in `PROFILE_REPORT.md` |
> | **A4** Build verification | ✅ DONE | `:app:assembleDebug` BUILD SUCCESSFUL; 5 APKs (per-ABI+universal). Task is `assembleDebug` (no flavors) |
> | **B1** Wire source-api + compile | ✅ DONE | mavenLocal `source-api-android` (Blackyfi group) + `core:common` `27.1.12297006`; **`:src:all:mangadex:assembleRelease` → signed `mangadex-release.apk`** (cert `882811c2…`, local debug key). Merged to EXT `main` |
> | **B2** Inspector `sources[]` | ✅ DONE | `tools/inspector.py` static extractor; ran on the **real signed APK** → MangaDex `index.min.json` = 9 lang sources w/ correct ids (en=`2499283573021220255`); top-level `lang` fix |
> | **B3** Icon extraction + CI | ✅ DONE | `tools/extract_icons.py` + SHA-pinned `build_push.yml`; added shared `res-shared/ic_launcher` → **`repo/icon/eu.kanade.tachiyomi.extension.all.mangadex.png` produced** (6 icons, 0 skipped) |
> | **B4** Signing/CI runbook (PREP) | ✅ DONE | `SIGNING_RUNBOOK.md`; 4 action SHAs resolved; no key generated |
> | **C1** MangaDex filters | ✅ DONE | filters + cover-quality `ConfigurableSource`; `compileReleaseKotlin` PASS |
> | **C2** MangaThemesia + 2 sites | ✅ DONE | theme assembles; KomikCast + ManhwaIndo compile |
> | **C3** Komga | ✅ DONE | HttpSource+ConfigurableSource; baseUrl/creds via prefs; compiles |
> | **C4** WeebCentral | ✅ DONE | HttpSource scraper, centralized selectors; compiles |
> | **D1** Unit tests | ✅ DONE | 11 tests GREEN (6 MangaDex DTO, 5 Madara selector) |
> | **D2** Security re-review | ✅ DONE | all Group C PASS; 3 non-blocking notes (F1/F2 info, F3 low) |
>
> EXT `main` fully integrated & green — all 6 source modules `assembleRelease` to **signed APKs**, themes assemble, 11 unit tests pass. **Nothing pushed; no repos made public; no PRODUCTION signing key generated.**
>
> **Q1 — repo owner: RESOLVED → `Blackyfi`.** Updater (A1), dep-mirror plan (A2), and the `source-api` mavenLocal/JitPack coordinate are all aligned to `com.github.Blackyfi.aniyomi-revived`.
> **Q2 — local debug keystore: RESOLVED → approved & done.** Generated a local throwaway debug keystore `ci.keystore` (gitignored, NOT the production key; alias `arext`, cert SHA-256 `882811c2205448e3914a866fe149dc9421a40659b39dbae01f5dc2dce6c4a5b0`). All 6 extension APKs signed with it; B1/B2/B3 APK-level acceptance fully closed. **The production signing key is still to be generated by you per `SIGNING_RUNBOOK.md`** before publishing.
>
> **Outward-facing rollout — 2026-05-29 (owner=Blackyfi):**
> - ✅ **EXT extension repo PUBLISHED & CI green.** `https://github.com/Blackyfi/aniyomi-revived-extensions` — push→build→sign→inspect→icons→index→publish workflow succeeds. **App-facing URL: `https://raw.githubusercontent.com/Blackyfi/aniyomi-revived-extensions/repo/index.min.json`**. `repo` branch has 6 signed APKs + 6 icons + `repo.json` (fingerprint `e4bbc0…ca9b`, so extensions load TRUSTED) + `index.min.json` (MangaDex shows 9 sources).
> - ✅ **source-api-on-CI SOLVED via in-repo `:lib-stub`** — a compile-only stub mirroring the host `source-api`/`core:common` API; extensions compile on CI with no mavenLocal and no app build. Verified by a forced clean recompile with mavenLocal moved aside. (Local unit tests still use the real mavenLocal artifacts via `testImplementation`.)
> - ✅ **Production extension signing key generated + 4 CI secrets set** (`SIGNING_KEYSTORE_B64`/`SIGNING_STORE_PASSWORD`/`SIGNING_KEY_ALIAS=arext`/`SIGNING_KEY_PASSWORD`); cert `e4bbc0…ca9b` published in `repo.json`. Keystore = `ci.keystore` (gitignored).
> - ✅ **APP updater repoint pushed** to `Blackyfi/aniyomi-revived` `main`. Internal planning docs kept LOCAL.
> - ✅ **APP release CI configured** — org-gate flipped to `Blackyfi/aniyomi-revived` (4 steps), APP release keystore generated + 4 secrets set (`SIGNING_KEY`/`ALIAS`/`KEY_STORE_PASSWORD`/`KEY_PASSWORD`, cert `6d16c0…81ef`), tag `v0.18.1.2` pushed.
> - ✅ **APP release CI GREEN.** After the fork Actions were enabled, the `v0.18.1.2` tag build succeeded and created a **draft** release "Aniyomi v0.18.1.2" with 5 signed APKs (`aniyomi-v0.18.1.2.apk` universal + `-arm64-v8a`/`-armeabi-v7a`/`-x86`/`-x86_64`), matching the in-app updater's asset-name expectations. Publish the draft when ready; future releases = push a new `v*` tag.
> - ⬜ **Deferred:** fork the 5 JitPack deps (A2 plan). Not required — builds work via `:lib-stub` (CI) + existing upstreams (app).
>
> **🔐 Signing keys (passwords were reported in chat — store OFFLINE, NOT in this file):** EXT key = EXT-repo `ci.keystore`, alias `arext`, cert SHA-256 `e4bbc0829bf2b1ef674b4772407c93898253620c5bebca3b3ddb372b6863ca9b`. APP key = `Projects\app-release.keystore` (outside repos), alias `aniyomi`, cert SHA-256 `6d16c09f5deb491ba6be5b725e15828cb4eea198a29cde9a4523713617d481ef`. Passwords are intentionally NOT written here (this file lives in the public repo's working dir). Losing either keystore/password breaks its trust/update chain — back them up.

Two repos are in play:
- **APP**: `C:\Users\nicol\Documents\Projects\aniyomi-revived`
- **EXT**: `C:\Users\nicol\Documents\Projects\aniyomi-revived-extensions` (sibling; git-initialized, scaffolded)

Context for any agent: read `PROFILE_REPORT.md` (full technical/security profile) and
`EXTENSIONS_REPO_STARTER.md` in the APP repo first. Tooling already installed & current:
`gh` (authed as Blackyfi, repo+workflow scopes), `osv-scanner`, Temurin JDK 17, Python 3.12.
**Android SDK is NOT installed** — task P0 handles it.

Conventions:
- **[parallel-safe]** = touches an isolated set of files; can run concurrently with other parallel-safe tasks.
- **[serial]** / **depends:** = must wait for the named task(s).
- Each code task: branch off, keep changes scoped, do NOT push to GitHub or make repos public without explicit user approval (outward-facing). Local commits are fine.
- Cite `file:line`. Report acceptance-criteria results explicitly (ran / passed / output).

---

## P0 — Prerequisite (run FIRST, serially; everything tagged "needs-sdk" depends on it)

### P0. Install Android SDK + wire both repos to build
- **Repo:** machine-level + APP + EXT
- **Scope:** Install Android cmdline-tools (latest: `https://dl.google.com/android/repository/commandlinetools-win-14742923_latest.zip`) to `C:\Android\sdk`, accept licenses, install `platform-tools`, `platforms;android-35`, `build-tools;35.0.0`. Set `ANDROID_HOME` (user env) + write `local.properties` (`sdk.dir=...`) in both APP and EXT (gitignored).
- **Acceptance:** `sdkmanager --list` works; `aapt2 version` works; `APP\gradlew.bat -q help` succeeds.

---

## Group A — App hardening & verification (APP repo)

### A1. Repoint the in-app updater to the user's fork [parallel-safe]
- **File:** `app/src/main/java/eu/kanade/tachiyomi/data/updater/AppUpdateChecker.kt:43-59` (+ the GITHUB_REPO constants it reads).
- **Scope:** Change stable/preview repo from `aniyomiorg/aniyomi` to the user's fork (ask user for exact repo names; default `Blackyfi/aniyomi-revived` + `-preview`). Keep behavior identical otherwise.
- **Acceptance:** updater points at the fork; note any release-asset naming assumptions.

### A2. Mirror the git-pinned JitPack deps [parallel-safe]
- **Files:** `gradle/libs.versions.toml:33,42,50,51,59` (unifile, injekt, image-decoder, subsampling-scale-image-view, flexible-adapter).
- **Scope:** Produce a concrete plan (and, if user approves repo creation, fork/mirror under their org) to replace commit-hash pins with tags from mirrors the user controls. Do NOT create public repos without approval — deliver the plan + the exact catalog edits ready to apply.
- **Acceptance:** a table of dep → upstream → proposed mirror/tag → catalog diff.

### A3. Full transitive OSV scan [serial, depends: P0]
- **Scope:** Enable Gradle dependency locking via an `--init-script` (don't modify tracked build files), run `gradlew :app:dependencies --write-locks` (and other modules as feasible), then `osv-scanner scan source --lockfile <generated>`. Compare against the clean direct-dep result in `build/osv_catalog_scan.py`.
- **Acceptance:** transitive advisory count + any CVE IDs; append findings to `PROFILE_REPORT.md` appendix.

### A4. Build verification [serial, depends: P0]
- **Scope:** Build a debug variant (`gradlew :app:assembleDebug` or the appropriate flavor, e.g. `assembleStandardDebug`). Capture failures and root-cause them. Do NOT change app behavior to force a pass — report blockers.
- **Acceptance:** APK produced OR a precise blocker list with file:line.

---

## Group B — Extensions infrastructure (EXT repo)

### B1. Wire source-api & compile MangaDex + Madara [serial, depends: P0]
- **Scope:** Implement SETUP.md §2 (prefer Option B local-maven: `APP\gradlew :source-api:publishToMavenLocal`, add `mavenLocal()` to EXT `settings.gradle.kts`, set the real coordinate/version in `gradle/libs.versions.toml`). Fix any compile errors in `MangaDex.kt`/`Madara.kt` against the real API.
- **Acceptance:** `EXT\gradlew :src:all:mangadex:assembleRelease` produces a signed (debug-key ok for local) APK; `:lib-multisrc:madara` compiles.

### B2. Inspector step for `sources[]` [parallel-safe with B3+]
- **File:** `tools/create_repo.py` (+ a new inspector).
- **Scope:** After build, load each APK's dex to extract each Source's `id`/`lang`/`name`/`baseUrl` and fill the `sources[]` array (the keiyoushi/yuzono "Inspector" pattern). A JVM tool that loads classes against a stub, or parse-from-known-factory, both acceptable.
- **Acceptance:** `index.min.json` for MangaDex shows its language sources with correct ids.

### B3. Icon extraction to `repo/icon/<pkg>.png` [parallel-safe]
- **Scope:** Add a step (script + CI wiring in `.github/workflows/build_push.yml`) that pulls each APK's launcher icon into `repo/icon/<pkg>.png` (the app expects this path).
- **Acceptance:** running it on the MangaDex APK yields `repo/icon/eu.kanade.tachiyomi.extension.all.mangadex.png`.

### B4. Signing key + CI publish (PREP ONLY) [parallel-safe]
- **Scope:** Document + script the keystore generation (SETUP.md §3) and the exact `gh secret set` commands. Do NOT generate/commit a key or push the `repo` branch without user approval. Produce a runbook the user executes.
- **Acceptance:** a step-by-step runbook + verified CI yaml (actions pinned to SHAs).

---

## Group C — New extensions (EXT repo) — all [parallel-safe] with each other (separate module dirs)

> Each lives in its own `src/<lang>/<name>/` or `lib-multisrc/<theme>/` dir → no file conflicts.
> Follow `CONTRIBUTING.md` (zero obfuscation, reviewed network calls, least privilege).

### C1. Expand MangaDex filters
- **Files:** `src/all/mangadex/...` — add `getFilterList()` (tags, content rating, status, sort) and apply in `searchMangaRequest`. Add cover-quality preference via `ConfigurableSource`.
- **Acceptance:** filters compile and map to API query params; documented.

### C2. MangaThemesia theme base + 2 sites
- **Files:** new `lib-multisrc/mangathemesia/...` + two `src/<lang>/<site>/` overrides.
- **Acceptance:** theme compiles; each site is a ~30-line override with centralized selectors.

### C3. Komga source (self-hosted)
- **Files:** new `src/all/komga/...` — `HttpSource` + `ConfigurableSource` (user-set base URL + credentials). JSON API.
- **Acceptance:** compiles; baseUrl/login via preferences; no hardcoded servers.

### C4. WeebCentral source
- **Files:** new `src/en/weebcentral/...` — standalone scraper (`HttpSource`, jsoup where needed).
- **Acceptance:** compiles; selectors centralized; `setUrlWithoutDomain` used.

---

## Group D — Quality (both repos) [parallel-safe]

### D1. Unit tests for extension parsing
- **Scope:** Add JVM unit tests for MangaDex DTO parsing (sample JSON fixtures) and Madara selector mapping (sample HTML fixtures) in EXT.
- **Acceptance:** `gradlew test` runs green for the new tests.

### D2. Security re-review of all new extension code
- **Scope:** Apply the `CONTRIBUTING.md` checklist to every new source from Group C; flag any network call not tracing to a literal/baseUrl, any reflection, any obfuscation.
- **Acceptance:** a per-source checklist with pass/fail.

---

## Suggested concurrency plan
1. **P0** alone (blocks build tasks).
2. Then fan out: **A1, A2, B3, B4, C1, C2, C3, C4, D2** (all parallel-safe, no SDK needed except none of these build) — plus **A3, A4, B1** once P0 done.
3. **B2** after B1 (needs built APK). **D1** after B1.
