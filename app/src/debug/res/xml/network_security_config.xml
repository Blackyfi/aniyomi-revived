<?xml version="1.0" encoding="utf-8"?>
<network-security-config xmlns:tools="http://schemas.android.com/tools">
    <!-- Debug-only config: additionally trust user-added CAs so HTTPS can be inspected with
         a local proxy (e.g. Charles/mitmproxy). Release builds use the stricter
         src/main/res/xml/network_security_config.xml which trusts system CAs only. -->
    <!-- Need to allow cleartext traffic for some sources -->
    <base-config
        cleartextTrafficPermitted="true"
        tools:ignore="InsecureBaseConfiguration">
        <trust-anchors>
            <!-- Trust preinstalled CAs -->
            <certificates src="system" />

            <!-- Additionally trust user added CAs -->
            <certificates
                src="user"
                tools:ignore="AcceptsUserCertificates" />
        </trust-anchors>
    </base-config>
</network-security-config>
